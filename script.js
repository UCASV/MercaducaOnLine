document.addEventListener("DOMContentLoaded", () => {

    // Funciones de barra de navegacion
    inicio(
    );
    emprendimientos(
    );
    masVendidos();
    proximos();
    contacto();

    window.inicio = inicio
    window.emprendimientos = emprendimientos
    window.masVendidos = masVendidos
    window.proximos = proximos
    window.contacto = contacto

    /////////////////////////////////////////////////////
});